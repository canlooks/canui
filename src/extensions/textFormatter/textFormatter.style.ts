import {css} from '@emotion/react'
import {defineInnerClasses, defineCss} from '../../utils'

export const classes = defineInnerClasses('text-formatter')

export const style = defineCss(({spacing}) => css`
`)